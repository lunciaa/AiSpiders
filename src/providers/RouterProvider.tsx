import { RouterProvider as _RouterProvider } from "react-router-dom"

import router from "@/routes/router"

const RouterProvider = () => <_RouterProvider router={router} />

export default RouterProvider
